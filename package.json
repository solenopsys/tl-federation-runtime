{
  "name": "@solenopsys/vite-federation-entry",
  "type": "module",
  "version": "0.0.0",
  "description": "Vite plugin to create a federated entry point",
  "main": "index.ts",
  "dependencies": {
    "zone.js": "^0.13.1"
  },
  "scripts": {
    "build": "esbuild index.ts --bundle --outfile=out.js --format=esm"
  },
  "devDependencies": {
    "esbuild": "^0.18.17"
  }
}